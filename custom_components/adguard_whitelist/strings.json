{
  "config": {
    "step": {
      "user": {
        "title": "DNS Filtering Backend",
        "description": "Choose how DNS filtering is managed.",
        "data": {
          "backend": "Backend"
        }
      },
      "adguard": {
        "title": "AdGuard Home Connection",
        "description": "Configure the connection to your AdGuard Home instance.",
        "data": {
          "adguard_url": "AdGuard Home URL",
          "adguard_user": "Username",
          "adguard_password": "Password",
          "client_ip": "Client IP address"
        },
        "data_description": {
          "adguard_url": "e.g. http://192.168.1.1:3000 or http://192.168.1.1:80",
          "client_ip": "IP of the device to manage (e.g. 192.168.1.50)"
        }
      },
      "ssh": {
        "title": "Firefox Bookmarks (Optional)",
        "description": "Optionally sync allowed sites as Firefox bookmarks via SSH.",
        "data": {
          "ssh_enabled": "Enable Firefox sync",
          "ssh_host": "SSH host",
          "ssh_port": "SSH port",
          "ssh_user": "SSH username",
          "ssh_password": "SSH password"
        }
      },
      "dnsmasq": {
        "title": "dnsmasq via SSH",
        "description": "Configure SSH access to the machine running dnsmasq.",
        "data": {
          "ssh_host": "SSH host (local IP)",
          "ssh_host_vpn": "SSH host VPN (optional)",
          "ssh_port": "SSH port",
          "ssh_user": "SSH username",
          "ssh_password": "SSH password",
          "dnsmasq_conf_path": "dnsmasq config file path",
          "upstream_dns": "Upstream DNS server",
          "firefox_sync": "Sync Firefox bookmarks"
        },
        "data_description": {
          "ssh_host": "e.g. 192.168.8.50",
          "ssh_host_vpn": "WireGuard VPN IP, used as fallback (e.g. 10.0.0.2)",
          "dnsmasq_conf_path": "e.g. /etc/dnsmasq.d/whitelist-camille.conf",
          "upstream_dns": "DNS server for allowed domains (e.g. 9.9.9.9)"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect. Check host/credentials.",
      "invalid_auth": "Invalid username or password.",
      "conf_not_found": "dnsmasq config file not found or empty."
    },
    "abort": {
      "already_configured": "This configuration is already set up."
    }
  },
  "options": {
    "step": {
      "adguard": {
        "title": "AdGuard Home Settings",
        "data": {
          "adguard_url": "AdGuard Home URL",
          "adguard_user": "Username",
          "adguard_password": "Password",
          "client_ip": "Client IP address"
        }
      },
      "ssh": {
        "title": "Firefox Bookmarks (Optional)",
        "data": {
          "ssh_enabled": "Enable Firefox sync",
          "ssh_host": "SSH host",
          "ssh_port": "SSH port",
          "ssh_user": "SSH username",
          "ssh_password": "SSH password"
        }
      },
      "dnsmasq": {
        "title": "dnsmasq Settings",
        "data": {
          "ssh_host": "SSH host (local IP)",
          "ssh_host_vpn": "SSH host VPN (optional)",
          "ssh_port": "SSH port",
          "ssh_user": "SSH username",
          "ssh_password": "SSH password",
          "dnsmasq_conf_path": "dnsmasq config file path",
          "upstream_dns": "Upstream DNS server",
          "firefox_sync": "Sync Firefox bookmarks"
        }
      }
    }
  }
}
